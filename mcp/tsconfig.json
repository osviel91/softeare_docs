{
  "extends": "../tsconfig.src.json",
  "//": "The stdio MCP host (ADR-039). It compiles its own sources together with every src/ module they reach.",
  "compilerOptions": {
    "lib": ["ES2023", "DOM"]
  },
  "include": [".", "../src/**/*.ts", "../apps/**/*.ts"]
}
